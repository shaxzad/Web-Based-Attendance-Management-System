# Environment Configuration Example
# Copy this file to .env and update values for your environment

# Development Environment (Local Docker)
ENVIRONMENT=development
DATABASE_URL=postgresql://postgres:password@localhost:5433/attendance_db

# Production Environment (Render.com)
# These are automatically set by render.yaml
# ENVIRONMENT=production
# DATABASE_URL=postgresql://user:pass@host:port/db (from Render database)

# Application Settings
SECRET_KEY=your-secret-key-here
ENVIRONMENT=development

# Database Settings (for local development)
POSTGRES_SERVER=localhost
POSTGRES_PORT=5433
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=attendance_db

# ZKTeco Device Settings (if needed)
ZKTECO_DEVICE_IP=
ZKTECO_DEVICE_PORT=4370
ZKTECO_DEVICE_PASSWORD=
